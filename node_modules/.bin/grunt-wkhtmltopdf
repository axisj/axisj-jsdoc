#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-wkhtmltopdf/bin/grunt-wkhtmltopdf" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-wkhtmltopdf/bin/grunt-wkhtmltopdf" "$@"
  ret=$?
fi
exit $ret
